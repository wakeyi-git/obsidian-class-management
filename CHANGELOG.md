# 변경 기록

이 프로젝트는 [Semantic Versioning](https://semver.org/)을 따릅니다.

## 1.59.0 - 2026-07-24

### 추가 — 주간학습안내 역편집 동선 · 재구성 기록 (개선 로드맵 Ⅱ-10·11)

- **주간학습안내 노트 상단 배너** — "이 노트는 스냅숏"임을 알리고 **`시간표에서 수정`**(그 주로 이동한 시간표·시수 뷰)과 **`이 주 다시 생성`** 버튼으로 원본 수정→재생성 동선을 잇습니다. 배너는 화면 전용이라 인쇄·PDF에는 나오지 않습니다
- **재구성 기록** — 차시를 수정·이동·통합한 의도를 진도표 비고에 `재구성: 사유`로 적으면 시간표·시수 뷰의 **재구성 기록** 표에 모입니다(기록이 있을 때만 표시, 전각 콜론 허용). 변경 전 원본 대비는 자동 스냅숏 백업이 담당 — 스쿨마스터 재구성현황 대응(ENHANCEMENT_PLAN 3.3·3.4 잔여 해소)
- 테스트 163→**164개**

### 추가 — 진단에 교육과정 점검 통합 · 시수 조절 제안 (개선 로드맵 Ⅱ-8·9)

- **데이터 진단이 교육과정 원장도 점검** — 기존 검사(중복 번호·링크·폴더)에 더해 ① 배정되지 않은 차시 ② 과목별 진도 시수 합 ≠ 수업 슬롯 수 ③ 기준 대비 시수 초과·미달 ④ 진도표 프로젝트·과제 링크 깨짐 ⑤ 단원 시작일>종료일을 함께 보고합니다 — `/vault-audit` 스킬 항목의 단독 경로판(스쿨마스터 '교육과정종합점검' 대응)
- **시수 점검에 "조절 제안"** — 초과·미달 과목을 짝지어 이번 학기 남은 수업 중 변경 후보 슬롯(날짜·교시)을 제안합니다. **제안만 하며 적용은 교사가 시간표 우클릭으로** — 저장 즉시 진도 재배정(1.31 원장 의미론 교훈 준수). 초과 과목이 없으면 기준·편성 확인 안내
- 테스트 162→**163개**(짝짓기·후보 부족 표시·미래 슬롯 없음)

### 추가 — 범교과 주제어 매핑 (개선 로드맵 Ⅱ-7)

- **진도표 비고 칸의 `#태그` 집계** — `#안전` `#인성`처럼 비고에 태그를 적으면 시간표·시수 뷰의 **범교과 주제어** 표가 주제어별 차시·시수(1·2학기·계)·과목별 시수를 집계합니다. 안전교육 이수 시수 확인 같은 실적 보고에 바로 쓸 수 있습니다
- **열 추가 없는 방식** — 기존 진도표 25개를 이행할 필요 없이 필요한 행에만 태그를 덧붙이면 됩니다(한 칸 여러 태그 가능, 같은 칸 중복 태그는 1차시). 태그는 옵시디언 태그로도 인식되어 태그 검색과도 호환됩니다
- 테스트 161→**162개**(집계·중복·정렬·학기 분리)

### 추가 — 학생 상담 자료 내보내기 (개선 로드맵 Ⅱ-6)

- **분석·보고서에 `상담 자료 내보내기` 버튼** — 기간·학생(1명)을 고르면 출결 집계·예외 내역, 학생 기록 원문(원본 링크), 미제출·보완 과제, 미회신 통신문, 상담 메모란(안건·학부모 의견·약속·후속)이 담긴 실명 문서를 `내보내기/`에 만듭니다
- 교사 본인 확인용 **비AI 경로**입니다 — AI 익명 내보내기와 반대 방향(외부로 나가지 않는 전제)이며, 인쇄 규격(cssclasses)이 적용되어 PDF 내보내기로 바로 A4가 됩니다
- 테스트 160→**161개** — 이 과정에서 용어 가드가 초안의 "학교생활 기록" 표기를 잡아 "학생 기록"으로 정정(§2 가드 실효 확인)

### 추가 — 학급 캘린더 진도 차시 레이어 (개선 로드맵 Ⅱ-5)

- **캘린더 "진도 차시" 토글** — 켜면 날짜마다 배정 차시가 교시·과목(주간은 학습 내용까지)으로 표시되고, 클릭하면 그 과목 진도표가 열립니다. 저장하지 않는 계산 레이어(원장 원칙)이며 기본은 꺼짐 — 켜짐 상태는 기억됩니다
- 월간 3차시·주간 8차시 초과분은 `+N차시`로 접습니다. 진도표·시간표 변경 시 캘린더도 함께 갱신됩니다
- **통합 목록 편입은 검토 후 제외** — 통합 목록은 파일 기반 기록 검색이 목적이라 연 986행의 파생 계산 행은 소음이라고 판단(실사용에서 필요가 확인되면 재검토, ENHANCEMENT_PLAN 3단계 잔여 항목 해소)
- 테스트 159→**160개**(날짜별 배정·휴업일 제외)

### 추가 — `할 일 자동 수집` 내장: task-scan 규칙의 플러그인 단독 경로 (개선 로드맵 Ⅰ-4)

- **규칙 7종을 core `task-scan.ts`로 내장** — 과제 D-7 준비 ◆ · 프로젝트 시작 D-7 ✦ · 행사 D-3 · 가정통신문 D-2 미회신 확인 · 시수 초과/미달 확인(주 1회) · 임박 단원의 설계 error 보완(상위 3건) · 백업 30일 경과(주 1회). 합계 상한 12건, 학생 이름은 제목에 넣지 않음
- **`할 일 자동 수집` 명령(44번째)** + 설정 → 데이터 보관의 **"할 일 자동 수집 (로드 시)"** 토글(기본 꺼짐)
- **sourceKey 멱등** — 예약 실행용 `/task-scan` 스킬과 키 규약이 같아 스킬·명령·자동 어느 조합으로 돌려도 같은 항목이 중복 생성되지 않습니다(완료·삭제한 할 일도 부활하지 않음)
- 테스트 153→**159개**(창 경계·멱등·상한·ISO 주차)

### 추가 — 실사용 대비 묶음: 인쇄 규격 · 피드백 기록 · 모바일 체크리스트 (개선 로드맵 Ⅰ-1·2·3)

- **주간학습안내·보고서 인쇄·PDF 규격** — 생성 노트에 `cssclasses: class-management-print`가 붙고, 인쇄·PDF 내보내기 시 A4 1장 규격이 적용됩니다(표 전폭·9pt·행 쪽 경계 보호, 속성 블록 숨김, 링크 잉크 통일). 화면 표시는 변하지 않으며, 기존 노트는 다시 생성하면 적용됩니다
- **`피드백 기록` 명령(43번째)** — 실사용 마찰을 그 자리에서 한 줄로 남기면 날짜·버전·현재 화면이 자동 스탬프되어 볼트 `학급운영/피드백.md`에 쌓입니다. '자주 쓰는 명령'에도 추가 가능. 트리아지 대장은 저장소 `docs/FEEDBACK.md`(2단계 실사용 검증 루프의 입구)
- **모바일 실기기 체크리스트** — TESTING.md에 개학 전 1회 수행할 폰·태블릿 점검 항목(길게 누르기·40px 타깃·IME·목표 시간 실측·PDF 규격) 신설

## 1.52.0 - 2026-07-24

### 변경 — AI 초안 흐름을 "익명 자료 내보내기"로 재편 · 기재요령 요약 규약 신설

- **분석·보고서의 `학생 피드백 초안`·`생활기록부 영역별 초안` 버튼 제거** — 플러그인은 LLM과 직접 연결되지 않으므로, 대신 **`AI 초안 자료 내보내기 (익명)`** 버튼이 기간·대상(학생 1명 또는 전체)의 학생 개별 기록을 **실명·파일 경로 없는** LLM 친화 마크다운 한 파일로 `내보내기/`에 생성. 작성 지침(사실 기반·낙인 금지·근거 인용), 학생별 [근거 NN] 목록(학생부 근거의 영역·교과·성취기준·평가요소·직접 관찰 필드 포함), 요청 출력 형식(피드백 + 창체/교과/행발 초안)이 담겨 외부 LLM에 바로 붙여넣을 수 있음
- **기재요령 요약 노트(kind `school-record-guideline`, 23종째)** — `기재요령 요약 노트` 버튼이 `교육과정/학생부 기재요령 요약.md` 스캐폴드를 생성(멱등, 설정의 기재요령 연도 사용). 학교 기재요령 PDF는 LLM 협업으로 요약해 채우며, **요약이 채워져 있으면 내보내기에 전문이 인라인**되어 외부 LLM이 그 기준을 지켜 작성함 — 설정의 "학생부 기재요령 연도"가 실제 동작에 연결됨
- AI 워크플로 지침(AI_WORKFLOW.md 생성분)에 기재요령 요약 노트 규약 추가, PRODUCT §9-1·§7, MANUAL 학기말 절, UX-FLOWS 워크플로 6 갱신
- 테스트 150→**153개**(익명성·경로 부재·기재요령 인라인·스캐폴드)

## 1.51.3 - 2026-07-24

### 수정 — 학생 타임라인 명칭 통일·넓은 화면 여백 해소

- 학급 메뉴 라벨·명령 이름이 "학생별 타임라인"으로 탭 제목("학생 타임라인")과 다르던 것을 **학생 타임라인**으로 통일 (학급 메뉴·명령 팔레트·빈 상태 제목)
- 타임라인 본문의 `max-width: 840px` 고정 폭을 제거 — 넓은 화면에서 오른쪽이 비지 않고 바디 폭을 사용 (§6.1 "콘텐츠 폭을 임의 고정 px로 제한하지 않는다" 위반 해소)

## 1.51.2 - 2026-07-24

### 수정 — 통합 목록 등 뷰 골격 깨짐 (레거시 selector 그룹 잔재)

- 학급 통합 목록이 오른쪽 절반에 구겨져 보이던 문제 — 뷰 **루트 클래스가 옛 공용 규칙의 selector 그룹에 함께 묶여** 있던 잔재 3곳이 골격의 세로 배치를 가로 flex로 덮어쓰고 있었음: ① 통합 목록 루트가 필터 바 패널 규칙에, ② GTD·루틴·보고서 루트가 루틴 스트립 규칙에, ③ 뷰 루트 8종이 모바일 padding 규칙에. 루트를 전부 분리하고 모든 루트 블록에 변수·배경·포커스 규칙만 남도록 전수 확인

## 1.51.1 - 2026-07-24

### 수정 — 캘린더 툴바 아랫선 정렬

- 유형·학생 필터의 라벨이 컨트롤 위에 얹힌 2줄 구조라 탐색 버튼(◀ 오늘 ▶·주간/월간)과 아랫선이 어긋나던 문제 — 라벨을 인라인(한 줄)으로 바꿔 툴바 전체가 같은 줄에 서게 함(라벨은 §7.3 규격대로 유지)

## 1.51.0 - 2026-07-24

### 추가 — 학급 대시보드를 아침 콕핏으로 확장

- **주(2):보조(1) 분할 7패널** — 주 열: **오늘 시간표**(교시·과목·배정 차시, 행 클릭→차시 인스펙터, 행사·휴업 반영) · **진행 중 단원**(오늘이 기간에 든 단원의 시수 진행률 막대 — 미니 로드맵, 통합 단원 ✦) · **최근 기록**. 보조 열: **확인 필요**(검토 대기 학생부 근거·미제출 과제·미회신 가정통신문·이번 달 출결 예외 건수) · **GTD 할 일**(상태별 미결 + 마감 지난·오늘 항목) · **다가오는 일정**(7일 내 행사 ●·휴업일) · **학생 명단**
- 각 패널은 하나의 질문에 답하고 해당 뷰로 가는 명시 버튼을 갖습니다(§7.2) — 시간표·시수 / 교육과정 로드맵 / 분석·보고서 / GTD 보드 / 학급 캘린더
- 학사일정·기초시간표가 없으면 각 패널이 다음 행동(노트 열기)을 안내합니다

## 1.50.0 - 2026-07-24

### 개선 — 뷰 레이아웃 표준화: 3층 골격·분할 기하 통일 (DESIGN §6.5 신설)

- **모든 중앙 뷰(12개)가 같은 3층 골격** — 헤더(제목·한 줄 설명·전역 행동)와 툴바(필터/기간 탐색)는 **스크롤해도 고정**되고, 본문(바디)이 유일한 스크롤러가 됨. `scaffoldView` 공용 헬퍼 신설, 서로 다르던 헤더 CSS 3벌(`header`/`view-heading`/`view-header`) 통합
- **헤더 요소 계약** — 제목은 탭 이름과 동일(예외 2곳 명문화: 대시보드=학급 이름, 타임라인=학생 이름). 이에 따라 단원 설계 뷰 제목 "교육과정-수업-평가-기록"→**"단원 설계 및 운영"**, 시간표·시수와 교육과정 로드맵에는 없던 표준 헤더(설명 포함) 신설, 캘린더의 월 표시·탐색·모드 전환은 고정 툴바로 이동
- **분할 기하 4종으로 통일** — 요약 지표 `auto-fit minmax(150px)`(3·4·6열 변형 제거) · 동등 패널 `auto-fit minmax(280px)` · 주+보조 **2:1**(보조 최소 260px — 루틴 1.75:1, 대시보드 1:1.6이던 임의 비율 정리) · 보드 열 최소 300px(카드)/190px(조밀)
- 루트 개별 padding·overflow 선언 제거(골격이 담당), 통합 목록 스크롤 보존을 바디 기준으로 전환, 타임라인 빈 상태도 표준 골격 사용

## 1.49.0 - 2026-07-24

### 추가 — 성취기준 노트 플러그인 인식: 검색·연결 UI (로드맵 R2 완주)

- **성취기준 파싱** — 성취기준 노트의 코드·전문·과목·학년군을 플러그인이 되읽음(`parseAchievementStandard`, 판별자 자기검증). "볼트 데이터, 플러그인 미파싱" 경계가 해소됨
- **검색 삽입 버튼** — 단원 설계 모달·학생부 근거 모달(개별/일괄)의 성취기준 칸 옆 🔍: 코드·내용·과목 퍼지 검색으로 노트에서 골라 `[코드] 전문`을 덧붙임(입력 중 내용 보존, 중복 코드는 안내). 성취기준 노트가 없으면 `성취기준 노트 생성` 명령을 안내
- **터치(모바일) 상호작용 확인** — 우클릭 편집 메뉴가 있는 화면(시간표·시수, 오늘, 교육과정 로드맵) 전부에 길게 누르기 등가가 이미 구현되어 있음을 확인하고 R2 완료로 확정 — **PRODUCT §10 R2 전 항목 완료**
- 테스트 147→**150개**(파싱·판별자·왕복)

## 1.48.0 - 2026-07-24

### 개선 — 부분 갱신 2단계: 범주별 영향 뷰 갱신 · 활동 색인 증분화 (§7 백로그)

- **영향 뷰만 갱신** — 볼트 변경 경로를 범주 18종으로 분류(코어 `change-scope`, 순수 함수·테스트)하고, 뷰 레지스트리의 `dependsOn`이 겹치는 뷰만 다시 그림. 출결 체크가 시간표·시수/로드맵을 건드리지 않고, 진도표 편집이 통합 목록을 건드리지 않음. 오늘·대시보드·인스펙터·보고서 같은 교차 도메인 콕핏은 안전하게 광역 유지
- **활동 색인 파일 단위 증분** — 파일별 mtime 캐시로 재빌드가 변경 파일만 다시 읽음(삭제·이동·프로필 전환은 자동 정리). 색인 무효화도 색인 원천 7범주(기록·출결·과제·할 일·회신·루틴·수업일지) 변경 시에만
- **이름 변경(rename) 이벤트 구독 신설** — 기존에는 노트 이름 변경이 뷰 갱신을 유발하지 않던 공백을 해소(이전·이후 경로 모두 분류)
- 테스트 142→**147개**(경로 분류·색인 범주 가드)

## 1.47.0 - 2026-07-24

### 개선 — 부분 갱신 1단계 · 파서 자기검증 · NoteKind 단일화 (§7 백로그 완주)

- **부분 갱신 1단계** — 기본 폴더 밖 볼트 변경(일기·첨부 등)은 학급 뷰 전체 재렌더를 건너뜀. 시간표·시수 뷰의 주 이동(◀·이번 주·▶)은 학기 데이터를 다시 읽지 않고 캐시로 즉시 다시 그림. 통합 목록은 볼트 변경 재렌더에도 스크롤 위치와 "더 보기"로 늘린 표시 한도를 보존
- **파서 판별자 자기검증** — 진도표·학사일정·기초시간표·기준 시수 파서가 frontmatter `class-management` 판별자를 스스로 확인하고 불일치 시 null(단원·수업일지·근거 파서와 계약 통일)
- **NoteKind 단일화** — kind 22종을 `core/types`의 `NOTE_KINDS` 상수로 명문화하고, 소스의 판별자 리터럴이 목록을 벗어나면 실패하는 가드 테스트 추가
- 소형 3건 — 저장소 경로 조립 51곳에 옵시디언 `normalizePath` 일괄 적용, terminology 가드의 문자열 연결(`"수업 " + "기록"`) 우회 봉합, 진도표에서 구분행이 헤더보다 먼저 나올 때 구버전 배치로 오분류되던 함정 제거
- 테스트 135→**142개**(판별자 거부·구분행·NoteKind 가드)

## 1.46.0 - 2026-07-24

### 개선 — ESLint 도입 (§7 백로그)

- **typescript-eslint 타입 인지 린트** — TS 레이어(src·packages/core) 대상, strict tsc가 못 잡는 부류를 겨냥: `no-floating-promises`·`no-misused-promises`·`await-thenable`·`no-unused-vars`. `npm run lint` 신설, `npm run check`에 편입되어 CI에서 자동 게이트
- 발견 위반 10건 해소 — 옵시디언 Setting·컴포넌트가 체이닝용 `.then()` 탓에 thenable로 취급되는 void 콜백 축약 반환 4곳(중괄호로 값 버림), 빈 인터페이스 1곳(type 별칭으로), 미사용 심벌 정리
- CLAUDE.md 검증 절차에 린트 추가

## 1.45.0 - 2026-07-24

### 개선 — main.ts 분해 2단계 · 볼트 IO 단일 창구 완성 (§7 백로그)

- **main.ts 2,043→약 1,000줄** — 명령 팔레트 42개 등록을 `commands.ts`로, 교육과정 도메인 플로 23개(진도 배정·주간안내·성취기준 스캐폴드/링크화·차시 고정·시간표 변경 재배정·수업일지/근거 흐름·raw 스탬프 등)를 `curriculum-flows.ts`로 추출. 뷰가 쓰는 진입점은 플러그인 위임 메서드로 유지되어 동작 불변
- **볼트 IO 단일 창구 완성** — maintenance.ts의 백업·자동 스냅숏·누락 복구·레거시 이행을 repository 메서드로 이관하고 파일 삭제, 보존 정리의 휴지통 이동도 repository(`trashFilesByPath`) 경유로. 쓰기 경로 vault 호출이 repository 밖 **0건**(CLAUDE.md 경계 규칙이 코드 전체에서 성립)
- 설정·프로필 관리는 플러그인 상태 소유와 결합돼 main.ts 잔류로 결정(§7 백로그에 기록)

## 1.44.0 - 2026-07-24

### 개선 — 전체 검토 반영: 쓰기 안전·코어 경계·뷰 수명주기

- **쓰기 시점 재파싱 전환** — 진도표 5개 쓰기(행 추가·📌 고정·배정 기입·링크 기입·성취기준 링크화)와 단원·수업일지 갱신이 파싱 시점 스냅숏으로 파일 전체를 덮어쓰던 것을, 쓰는 순간의 실제 내용을 다시 읽어 반영하는 방식(`vault.process`)으로 — 모달·배정 사이에 한 손편집이 조용히 사라지지 않음
- **단원·수업일지 수동 절 보존** — 모달 재저장 시 스캐폴드에 없는 `##` 절(교사가 덧붙인 메모 등)을 보존(과제 확인표와 같은 규칙, `mergeManualSections`)
- **코어 순수성 실질화** — DOM 헬퍼(`dom.ts`)를 플러그인 층으로 이동, AI 협업의 볼트 IO(작업 공간 생성·초안 생성)를 저장소로 이관(코어에는 순수 빌더만 잔류), 순수성 가드를 소스 전체 스캔으로 강화(다중 행 import·export-from·동적 import·DOM/볼트 런타임 토큰 금지)
- **뷰 수명주기 정리** — 뷰 16종의 등록·볼트 변경 갱신을 레지스트리 하나(`MANAGED_VIEWS`)로 통합, `onunload`의 leaf detach 제거(옵시디언 가이드라인 — 업데이트·비활성화 시 사용자 레이아웃 보존, 기존 6종 누락 불일치도 해소), 시작 지연 작업 타이머 정리 등록
- 소형 결함 4건 — 활동 색인이 빌드 중 볼트 변경을 놓치던 한 사이클 스테일(세대 가드), frontmatter 문자열 정규화 4벌 통합(trim 일원화), 학생 링크 셀의 손편집 일반 `|` 별칭 허용, 단원 ID 생성 주입 가능화
- 테스트 116→**135개** — entity-notes(Bases 정의 단일 진실)·utils 직접 테스트 신설, 수동 절 보존·왕복 검증 추가
- 문서 현행화 — UX-FLOWS 뷰 이름(1.41.0 개명 반영)·명령 수 42, PRODUCT §5 수치, 스탬프 v1.44.0, ROADMAP·ENHANCEMENT_PLAN 현황 주석, UIUX §7 백로그에 검토 발견 미착수 항목 7건 기록

## 1.43.0 - 2026-07-23

### 추가 — 자주 쓰는 명령 사용자 구성

- 학급 메뉴 하단 "자주 쓰는 명령"을 **설정에서 직접 구성** — 설정 탭의 "자주 쓰는 명령" 목록에서 **+로 추가(검색 선택)·삭제·드래그 정렬**, 변경 즉시 학급 메뉴에 반영
- 고를 수 있는 동작 14종: 출결 체크 · 학생 빠른 기록 · 과제 체크 · 할 일 빠른 수집 · 가정통신문 회신 · 학생 개별 기록 · 학급 일괄 기록 · 주간학습안내 생성 · 차시 인스펙터 · 학생 인스펙터 · 학생 추가·명렬표 · 새 통합 단원 설계 · 진도표 차시 가져오기 · 진도 자동 배정
- 기본 구성은 기존 4종(출결·기록·과제·할 일) 그대로, 현재 화면 작업과 겹치는 항목은 이전처럼 자동 숨김

## 1.42.0 - 2026-07-23

### 개선 — 설정 탭을 Obsidian 1.13 선언형으로 전환 (공식 지침 반영)

- **선언형 정의(getSettingDefinitions)로 전면 이행** — 렌더·저장을 프레임워크가 담당(즉시 저장), 설정 검색과 자동 호환
- **폴더 선택기** — 기본 폴더·AI 결과 폴더(볼트 경로 2곳)는 `folder` 컨트롤로 볼트 폴더 제안을 받으며 입력. 하위 폴더명(학생·과제 등)은 이름이므로 텍스트 유지 + **금지 문자 검증**(\ : * ? " < > | · 앞뒤 공백) 인라인 오류 표시
- **검증 추가** — 학년도 네 자리 검사, 보관 기간 1년 미만 거부
- **지침 정합** — 첫 구역은 제목 없이 시작(불필요한 최상단 헤딩 제거), "학급·학기 추가 및 전환"은 안내문 대신 **클릭하면 바로 열리는 동작 행**으로
- 학년 변경 시 교과 목록 재설정·화면 갱신, 교과 목록(배열↔쉼표 문자열) 변환은 커스텀 값 어댑터로 처리 — 동작 불변

## 1.41.1 - 2026-07-23

### 수정 — 단원 카드 제목이 링크 스타일을 못 받던 문제

- 옵시디언 앱 기본 버튼 규칙(`button:not(.clickable-icon)`)이 제목 버튼 선택자보다 특이도가 높아 회색 배경·검정색이 남던 문제 — 카드 범위 선택자로 특이도를 올려 링크 색·투명 배경·호버 규칙을 확정(GTD 제목과 동일한 계층)

## 1.41.0 - 2026-07-23

### 변경 — 학급 메뉴 그룹화·뷰 개명

- **뷰 목록을 3그룹으로**: [학급 운영](대시보드·캘린더·통합 목록·학생별 타임라인·GTD 할 일·루틴 체크리스트) / [교육과정-수업-평가-기록](시간표·시수, 단원 설계 및 운영, 교육과정 로드맵, 분석·보고서) / [데이터·유지관리](학급·데이터, 백업·유지관리)
- **개명 3건**(내부 식별자 유지): 교육과정 운영 → **시간표·시수** · 교육과정 일체화 → **단원 설계 및 운영** · 일체화 로드맵 → **교육과정 로드맵** — 명령 팔레트·뷰 제목·문서 동기화
- **루틴은 GTD와 통합하지 않고 유지** — 대신 GTD 보드 상단에 **오늘 루틴 n/m 완료** 요약과 열기 버튼(수업일에 루틴이 있을 때만)

## 1.40.0 - 2026-07-23

### 개선 — 시수 점검 학기별 기준·명칭 통일·내비 즉시 강조

- **시수 점검이 학기별 기준-편성-실행 3단 대조로** — 표가 1학기·2학기·학년 세 묶음(각각 기준|편성|실행)으로 재편. 기준 시수 노트가 5열(`구분|교과·영역|1학기|2학기|학년`)이 되고 학년 칸을 비우면 1·2학기 합으로 계산, 구형 2·3열 노트도 계속 읽힘(학년 기준만). 볼트 노트 이행: 1·2학기 기준을 학기 편성값으로 채움 — 491+495=986 전 과목 학년 기준과 일치 검증
- **학생부 근거 동작 명칭 통일** — 화면마다 다르던 이름(개별 근거 기록/학생 근거/학생부 근거 기록 · 학급 일괄 입력/학급 근거/학생부 학급 일괄)을 **학생 개별 기록 / 학급 일괄 기록**으로 — 보고서·일체화 카드·학급 메뉴·명령 팔레트 전부
- **학급·데이터 작업에 `학급·학기 추가 및 전환` 추가** (왼쪽 학급 메뉴)
- **내비 활성 강조 즉시 반영** — 뷰 이름을 클릭하는 순간 강조가 옮겨감(뷰 포커스 이벤트가 늦게 오는 경우 대비)
- **링크 텍스트 마무리** — 차시 인스펙터의 연결된 단원·✦ 프로젝트·◆ 과제·📖 지도서 항목도 링크 스타일(강조색·호버 밑줄), 단원 카드 제목 색을 링크 색으로 확정
- 테스트 116개

## 1.39.7 - 2026-07-23

### 변경 — "일체화 간트" → "일체화 로드맵" · 제목 버튼 배경 잔상 수정

- 뷰·명령·학급 메뉴·문서의 표시 이름을 **일체화 로드맵**으로 — "교육과정 일체화"와 짝을 이루고 도구 용어(간트) 제거. 내부 식별자·CSS는 유지(호환)
- 단원·GTD 카드 **제목 버튼에 마우스를 올리면 옵시디언 기본 버튼의 회색 배경이 남던 문제** 수정 — 호버·활성 상태 배경 제거(밑줄·색 변화만 링크 규칙대로)

## 1.39.6 - 2026-07-23

### 수정 — 카드 버튼 크기·기간 잘림 마무리·링크 텍스트 일관화

- 카드 버튼을 편한 크기로(작은 글씨 압축 해제), 열 최소 폭 360px로 흡수 — 한 줄 유지
- **지표 그리드가 좁으면 2×2로 흐르도록** 칸 최소 폭 상향 — 기간이 어떤 폭에서도 잘리지 않음
- **클릭 가능한 텍스트를 옵시디언 링크 스타일로 통일** — 공용 `link-text` 스타일이 테마의 링크 변수(`--link-color`·`--link-decoration` 계열)를 그대로 사용: 단원 카드 제목, GTD 카드 제목, 차시 인스펙터 성취기준 칩. 테마·호버 밑줄 설정을 따르므로 볼트 어디서든 같은 링크 감각

## 1.39.5 - 2026-07-23

### 수정 — 단원 카드 기간 잘림·버튼 두 줄

- **기간 표기를 축약**(예: `8/19~10/29`) — 지표 칸 폭에서 연도 포함 전체 날짜가 잘리던 문제를 표기 차원에서 해소(학년도는 화면 맥락에 있음)
- **버튼 한 줄화** — GTD 카드 규약대로 **단원명 제목이 곧 노트 열기 버튼**이 되어 "노트 열기" 버튼을 흡수(5→4개), 남은 버튼(설계 수정·수업일지·학생 근거·학급 근거)은 압축형 한 줄 고정. 열 최소 폭 340px로 소폭 확대
- 제목 버튼 키보드 접근(포커스 표시·aria-label) 포함

## 1.39.4 - 2026-07-23

### 수정 — 단원 카드 지표 칸 복원·연결도 필터 형태

- 카드의 **기간·시수·차시·학생 근거 지표가 칸 구분 없이** 풀려 보이던 것을 복원 — 지표마다 테두리·배경(secondary) 칸(1.39.2에서 카드면이 primary가 되며 대비가 사라졌던 부분)
- **연결도 필터를 선택 상자로** — 길게 늘어진 체크박스 대신 다른 필터와 같은 형태: 전체 / 미완만(100% 미만)

## 1.39.3 - 2026-07-23

### 수정 — 검색 중 한글 조합이 끊기던 문제 (일체화·GTD)

- 일체화·GTD 검색이 글쇠마다 **뷰 전체를 다시 그려** 입력창이 재생성되면서 한글 조합("곱" 입력 중 "ㄱ")이 끊기고 포커스가 빠지며, 조합 중 낱자로 필터가 걸려 카드가 전부 사라지던 문제 수정
- 필터 바는 그대로 두고 **보드 영역만 다시 그리도록** 분리 — 입력창의 조합·포커스가 보존되고 결과가 타자 속도에 맞게 갱신됨(통합 목록과 같은 구조)

## 1.39.2 - 2026-07-23

### 수정 — 일체화 단원 카드 대비

- 칸반 열 배경(secondary)과 같은 색이던 단원 카드면을 primary로 — GTD 보드 카드와 같은 대비 규약, 테두리만으로 구분되던 문제 해소

## 1.39.1 - 2026-07-23

### 수정 — 간트 오늘 칩의 행정 학기 반영

- 보고 있는 학기 범위만 검사해 방학 중 "학기 범위 밖(방학)"으로 뭉뚱그리던 것을, 행정 학기 기준으로 정확히 표시:
  - 오늘이 보는 학기 안이면 `오늘 …` (방학 구간이면 ` · 여름방학` 덧붙임 — 오늘 선도 그려짐)
  - 다른 학기면 ` · 1학기(여름방학)`처럼 소속 학기·방학 이름 표시
- 1학기 간트를 열면 오늘 선이 여름방학 위치에 정상 표시됨(행정 학기 범위 안이므로)

## 1.39.0 - 2026-07-23

### 추가 — 학사일정 방학 구간 (행정 학기 지원)

- 학사일정 노트에 **`## 방학` 표**(시작|종료|이름) 도입 — 행정 학기(1학기=여름방학 포함, 2학기=겨울방학 포함)로 학기 시작·종료를 적어도 방학 기간은 비수업일로 판정
- 수업일 판정(dayStatus) 한 곳에 반영되므로 **주간 시간표 배정·진도 자동 배정·시수 편성·수업일수 계산·루틴 생성이 모두 방학을 제외** — 방학 표가 없는 노트는 기존 파생 방학(학기 사이 기간) 그대로
- 학기 전환 제안을 수업일에만 — 행정 학기에서는 방학도 학기 범위 안이라 방학 중 오탐 방지
- 새 학사일정 스캐폴드에 방학 절 포함, 볼트 노트에 방학 표 추가(여름 7/21~8/18·겨울 1/9~2/28) 및 안내문 현행화 — **수업일수 1학기 94·2학기 96으로 목표와 일치 검증**
- 테스트 115개

## 1.38.0 - 2026-07-23

### 개선 — 설정 탭 정비

- **그룹 재편**: 학급 정보 / 폴더 / 데이터 보관 / AI 협업과 개인정보 — 관련 항목끼리 묶고 각 그룹에 안내문
- **라벨·설명 현행화**: "기록 폴더"→**학생 기록 폴더**, 기본 폴더·교육과정 폴더 설명에 실제 하위 구조(단원·수업일지·모아보기 등 고정 이름) 명시, 설명 없던 항목 전부에 용도 한 줄
- **학기 옵션 정리**: 여름방학·겨울방학 선택지 제거 — 시수·진도·간트가 모두 1·2학기 축이라 혼란만 유발. 방학 중에는 준비 중인 학기를 선택하도록 안내(학기 전환 제안 알림과 연동)
- 안내 보강: 여러 학급은 `학급·학기 추가 및 전환` 명령, 폴더 변경 시 기존 노트 이동 필요, 백업 폴더의 자동 스냅숏 설명, 익명화 권장 문구
- 문자열 설정 헬퍼로 중복 코드 정리(동작 불변)

## 1.37.2 - 2026-07-23

### 수정 — 방학 중 루틴 노트 자동 생성 중지

- 루틴 인스턴스를 **수업일에만** 자동 생성 — 주말·휴업일·방학은 건너뜀(학사일정 기준, 학사일정이 없으면 기존 동작 유지). 이미 만들어진 노트 열람은 그대로
- 루틴 뷰의 비수업일 안내 문구 구분("주말·휴업일·방학에는 루틴을 만들지 않습니다")
- 볼트 정리: 방학 중 자동 생성됐던 루틴 노트 3개(7/21~23, 체크 흔적 없음) 백업 후 삭제

## 1.37.1 - 2026-07-23

### 수정 — 활동 유형 "생활 기록" → "학생 기록" (폴더명 정합)

- 캘린더·통합 목록 유형, 타임라인 요약, 최근 기록 없음 모달, 보고서(표 헤더·요약문), 진단 문구 등 9곳을 폴더명(`학생 기록/`)과 같은 **학생 기록**으로 통일
- §2 용어 사전에 "학생 기록"(record 총칭) 행 추가, 용어 가드에 "생활 기록" 금지 규칙 추가

## 1.37.0 - 2026-07-23

### 변경 — 일체화 필터 재편 (칸반 보드에 맞게)

- **상태 필터 제거** — 칸반 열이 곧 상태라 중복
- 대신 카드 탐색에 필요한 필터 추가(통합 목록과 같은 필터 바·항상 보이는 라벨):
  - **검색** — 단원명·주제·성취기준 코드·교과 (예: "곱셈", "4과05-01")
  - **구분** — 전체/일반/프로젝트
  - **연결도 미완만** — 설계가 4단계 미충족(연결도<100%)인 카드만 — 개학 전 설계 마감 점검용
- 교과 필터는 유지, 전용 필터 CSS는 공용 필터 바로 통합

## 1.36.1 - 2026-07-23

### 수정 — 활동 유형 "수업 실행" → "수업일지" (§2 용어 정합)

- 캘린더·통합 목록의 유형 라벨과 단원 카드 섹션("차시 실행"), 설정 설명, 진단 문구를 표준어 **수업일지**로 통일
- 용어 가드에 "수업 실행"·"차시 실행" 금지 규칙 추가("실행 완료" 상태어는 예외), §2 표 갱신 — 같은 유의어가 다시 못 들어옴

## 1.36.0 - 2026-07-23

### 추가 — 학급 캘린더에 프로젝트 표시

- 프로젝트(통합 단원)의 **시작일·마침일**에 `✦ {프로젝트} 시작 / 마침` 항목 표시(강조색 점·클릭하면 단원 노트) — 주간 시간표·간트와 같은 ✦ 규약
- 기간 전체를 매일 반복 표시하지 않고 경계일만 표시(월간 화면 소음 방지). 과제·수업 실행 등 기존 유형은 그대로

## 1.35.0 - 2026-07-23

### 변경 — 교육과정 일체화 단원 카드를 칸반 보드로

- 단원 카드를 **설계 중 → 실행 준비 → 운영 중 → 운영 완료** 4열 보드로 배치
- **기간 기반 자동 이동** — 시작일이 되면 운영 중, 종료일이 지나면 운영 완료 열로 파생 배치(노트는 다시 쓰지 않음 — 원장=계산 원칙). 기간 전에는 노트의 설계 상태(설계 중/실행 준비)를 따름. 카드 상태 칩도 파생 단계 표시
- 열 안은 시작일 순 정렬, 빈 열 안내, 좁은 화면 가로 스크롤
- 할 일 직렬화(`taskMarkdown`)를 core로 승격 — 예약 수집 스킬이 플러그인과 같은 형식을 쓰기 위한 준비(sourceKey 등 추가 프론트매터 지원)

## 1.34.0 - 2026-07-22

### 추가 — 차시 인스펙터에서 성취기준 노트·지도서 열기

- **성취기준 코드가 클릭 칩으로** — 진도 차시 섹션의 성취기준 줄에서 코드를 클릭(Enter/Space)하면 해당 성취기준 노트가 열림. 노트가 없으면 `성취기준 노트 생성` 명령 안내
- **📖 지도서 항목** — 차시가 속한 단원 노트의 지도서 딥링크(`…pdf#page=N`)로 PDF를 해당 단원 시작 쪽에서 엶. 목차 데이터가 단원 단위라 차시 단위 쪽 정밀 이동은 하지 않음(추정 금지)
- core에 `pdfPageLinks` 추출 헬퍼 추가, 테스트 113개

## 1.33.0 - 2026-07-22

### 개선 — 시간표 마커 정렬·차시 인스펙터 연계 열기

- **주간 시간표 마커 가운데 정렬** — 과목·진도가 가운데 정렬인 칸에서 ✦·◆ 표식만 왼쪽으로 쏠리던 것을 맞춤(오늘 패널은 왼쪽 리스트라 그대로)
- **차시 인스펙터 진도 차시 섹션에 연계 열기 항목** — 배정 차시에 프로젝트·과제가 연계돼 있으면 `✦ 프로젝트 이름`·`◆ 과제 이름` 항목이 나타나고 클릭(Enter/Space)으로 해당 단원 노트·과제 노트를 바로 엶
- 연결된 단원 항목에도 키보드 접근(role·tabindex·Enter/Space·aria-label) 보강
- core에 `wikiLinkTarget`(링크 대상 추출 — wikiLinkText의 짝) 추가, 테스트 112개

## 1.32.1 - 2026-07-22

### 수정 — 주간 시간표 표 깨짐 (1.32.0 회귀)

- 마커 CSS를 넣을 때 주간 시간표·시수 점검이 공유하던 표 규칙의 선택자를 잘못 잘라, 주간 시간표가 `display: flex`를 받아 머리글·본문이 옆으로 갈라지던 문제 수정 — 공유 표 규칙 복원, 마커 규칙은 단독 선택자로 분리

## 1.32.0 - 2026-07-22

### 추가 — 주간 시간표·오늘 패널에 프로젝트·과제 표시

- 배정 차시가 프로젝트에 이관·연계돼 있으면 칸에 **✦ 프로젝트 이름**(강조색), 과제가 연계돼 있으면 **◆ 과제**(주황)를 진도 아래에 표시 — 마우스를 올리면 과제 이름 전체
- 교육과정 운영 주간 시간표와 오른쪽 오늘 패널 공통(공용 렌더러), 칸 aria-label에도 프로젝트·과제 정보 포함
- 표식은 정보 표시이며 이동은 기존 경로(차시 인스펙터) 그대로

## 1.31.2 - 2026-07-22

### 수정 — 0시수 단원이 1시수로 계산되던 문제

- 전량 통합 이관된 일반 단원(`plannedHours: 0`)을 파서가 무효로 보고 기본값 1로 되돌려, 일체화 뷰 운영 시수가 편성보다 13시간 크게(508/495) 표시되던 문제 수정
- 파서·직렬화·단원 설계 모달 모두 0시수 허용(값이 없을 때만 기본 1) — 왕복 테스트 추가, 111개

## 1.31.1 - 2026-07-22

### 변경 — 단원 시수 원장 규칙 확정 (일반+프로젝트=편성)

- **규칙**: 일반 단원 시수 = 진도표 자체 운영분(통합 이관 차시 제외), 프로젝트 단원 시수 = 이관행 시수 합 — 진도표의 모든 행이 정확히 한 단원에 계상되어 **일반 합 + 프로젝트 합 = 시간표 편성 시수**가 성립
- `일반 단원 일괄 생성`이 이 규칙으로 시수를 기록(이관 차시는 전개의 → 표시로만 남음)
- 다중 프로젝트 링크 행은 배정일이 드는 프로젝트로, 기간이 겹치면 더 좁은 기간이 우선
- **볼트 적용(2026 2학기)**: 일반 조정 14건·프로젝트 재계상 6건·계기/자율 편성 단원 12건 신규 — 일반 358 + 프로젝트 137 = **495 = 편성** 검증. 음악·체육 권역 노트는 전개의 차시 번호로 소속 행을 복원해 계산, 백업 보관
- PRODUCT 데이터 모델에 규칙 명시

## 1.31.0 - 2026-07-22

### 개선 — 시수 점검: 학기 분리·구분 소계·노트 순서 표시

- **편성·실행을 학기별 열로 분리** — 기준 | 1학기 편성 | 1학기 실행 | 2학기 편성 | 2학기 실행 | 편성 합 | 증감 | 상태 (증감·상태는 연간 편성 합 대비)
- **구분 소계·총계 행 추가** — 교과 소계·창체 소계(항목 2개 이상인 구분)와 총계를 굵게 표시. 기초시간표가 없는 학기는 0으로 표시(이전엔 제외 집계 안내만)
- **행 순서 = 기준 시수 노트의 표 순서** — 기준 시수 내림차순 정렬을 제거하고 학교 편성표 순서 그대로 표시
- **기준 시수 노트에 구분 열** — `| 구분 | 교과·영역 | 연간 기준 시수 |` 형식. 학교자율시간 과목·활동(예: 디지털 놀이터)을 행 추가로 등록하고 구분은 학교 편성표에 따라 지정. 구분 칸을 비우면 위 행을 승계(병합 셀식 편집), **구형 2열 표도 계속 읽힘**(창체 접두로 구분 추정)
- 볼트 기준 시수 노트 이행(백업·왕복 검증) — 기존 행 순서(국어→사회→도덕→수학→과학→디지털 놀이터→체육→음악→미술→영어→창체 3종)가 그대로 표시 순서가 됨
- 테스트 110개(시수 점검 6건 재작성)

## 1.30.0 - 2026-07-22

### 변경 — 볼트 폴더 구조 직관화

폴더 이름 4곳을 뜻이 드러나게 바꿨습니다 (계층 구조는 유지):

| 이전 | 이후 | 이유 |
|---|---|---|
| 루트 `교육과정/` | `국가 교육과정/` | `학급운영/교육과정/`(우리 반 운영)과 이름 충돌 해소 |
| `학급운영/기록/` | `학급운영/학생 기록/` | `학생/`(명렬)과 구분, 내용물 명확화 |
| `교육과정/설계/` | `교육과정/단원/` | 용어 사전 표준어 정합(단원 노트 49개) |
| `교육과정/뷰/` | `교육과정/모아보기/` | 기술 용어 제거(Bases 모아보기) |

- 기록 폴더는 설정값이므로 **설정 스키마 v13 마이그레이션** 추가 — 기본 이름("기록")을 쓰던 설정만 "학생 기록"으로 자동 전환(사용자 지정 이름은 유지)
- 로드 시 **구 이름 폴더 감지 안내** — 구 폴더가 있고 새 폴더가 없으면 이행 안내 알림(자동 이동은 하지 않음)
- **볼트 이행 완료**: 폴더 4개 이름 변경(항목 수 전후 동일 15·2·49·7), 경로 링크 재작성 12파일(`…/설계/`→`…/단원/`), 잔여 구경로 0, 새로 깨진 링크 0 검증. 이동 폴더 원본·재작성 노트 70개를 볼트 백업(`백업/… 자동`)과 스크래치에 이중 백업
- 문서·규약 동기화: PRODUCT 데이터 모델 경로, 볼트 AGENTS.md RAW 경로(모드 A), 스킬 3종(unit-scaffold·record-draft·standards-sync)

## 1.29.1 - 2026-07-22

### 수정 — 프론트매터 속성 편집기 호환 (깨져 보이던 속성)

- 옵시디언 속성 UI가 다루지 못해 `?`(주황 JSON)로 깨져 보이던 두 형태를 호환 형으로 변경:
  - **단원 노트 `conceptInquiryStrands`(객체 배열)** → JSON 문자열(따옴표 감싼 한 줄)로 저장
  - **학사일정 `weekdayPeriods`(숫자 배열)** → 쉼표 문자열("5, 6, 5, 6, 5")로 저장
- 파서는 구형(배열)도 계속 읽으므로 기존 노트 호환(왕복 테스트 2건 추가, 107개). processFrontMatter 재직렬화로 생기던 블록 YAML 드리프트(수학 단원에서 발생)도 문자열화로 재발 방지
- **볼트 이행 7건**(학사일정 1·통합 단원 6): 변경 전 원본을 볼트 백업 폴더와 스크래치에 이중 백업, YAML 파서 왕복 동치 검증 후에만 기록, 재실행 멱등. 이행 후 실파일 파서 검증 통과
- UIUX §데이터 안전에 "프론트매터는 속성 편집기 호환 형만" 규칙 추가(재발 방지)

## 1.29.0 - 2026-07-22

### 변경 — 진도표 열 헤더 "통합 단원" → "프로젝트" (백로그 마지막 항목)

- 열이 프로젝트(통합 단원) 링크 전용이라는 의미가 이름에 드러나도록 개명 — 헤더 상수·매처 갱신
- **하위 호환**: 열 위치를 헤더 이름으로 해석하므로 구명 "통합 단원" 헤더 노트도 그대로 읽힘(호환 테스트 추가, 105개)
- **볼트 이행**: 진도표 25개의 헤더 셀만 정밀 치환 — 변경 전 원본을 볼트 백업 폴더(`백업/… 자동`, 복구 화면 사용 가능)와 스크래치 사본으로 이중 백업, 치환 전·후 core 파서 행 완전 일치 검증(불일치 0), 재실행 멱등
- 이것으로 UIUX §7 리팩토링 백로그 전 항목 완료

## 1.28.0 - 2026-07-22

### 추가·개선 — 백로그 ○ 잔여 정리 (UIUX §7)

- **학기 전환 제안** — 플러그인 로드 시 오늘이 설정과 다른 학기 기간이면 전환 안내 알림(자동 변경은 하지 않음)
- **`수업일지 진도표 역링크 채우기` 명령** — 손으로 만든 수업일지도 플러그인 생성분처럼 진도표 비고에 `[[…|수업일지]]` 링크를 채움. 날짜·과목으로 배정 차시를 찾고 교시 일치 행 우선, 이미 연결·미배정 건수는 알림으로 구분 보고(멱등)
- **진도표 연속 기입 버그 수정** — 같은 표에 링크를 잇달아 기입할 때(한 날짜 여러 교시 과제 연계 등) 앞선 기입이 유실될 수 있던 문제: 쓰기 후 메모리 행을 갱신하도록 수정
- **aria-label 보강** — 대시보드 명단(학생 인스펙터)·타임라인·최근 기록 행, 보고서 커버리지 누락 버튼에 대상·동작을 담은 라벨
- **명령 이름 정합 7건** — 뷰 여는 명령을 내비게이터 라벨과 일치(교육과정 일체화·분석·보고서·백업·유지관리·학급·데이터·통합 목록), "학교생활기록부 근거…" 2건을 §2 표준어 "학생부 근거…"로. 접미 규칙은 패널 뷰만 `(오른쪽/왼쪽 패널)` 유지
- 보고서 뷰의 중복 필터 라벨 헬퍼를 `@core/dom`의 `filterLabel`로 통합
- 진도표 "통합 단원" 열명 변경은 데이터 이행이 필요해 백로그 보류 유지

## 1.27.0 - 2026-07-22

### 추가 — 대량 쓰기 전 자동 스냅숏 (백로그 ◐ / UIUX §5)

- **진도 자동 배정**·**진도표 성취기준 링크화**가 노트를 다시 쓰기 전에 대상 진도표만 담은 자동 스냅숏을 백업 폴더 규격으로 저장 — 폴더명 `YYYY-MM-DD HHMMSS 자동`, 백업 정보에 계기(trigger) 기록
- 스냅숏은 기존 백업 목록·누락 파일 복구 화면에서 그대로 보이고 쓸 수 있음. 스냅숏 실패 시 대량 쓰기를 진행하지 않음
- 링크화는 바뀔 표가 없으면 스냅숏 없이 안내만 표시(불필요한 백업 없음), 완료 알림에 `· 변경 전 스냅숏 저장` 명시(§3 문형)

## 1.26.1 - 2026-07-22

### 개선 — 용어 사전 가드 테스트 (백로그 ◐ / UIUX §2)

- `tests/terminology.test.mjs` 신설 — 소스의 사용자 노출 문자열 리터럴에서 금지 유의어(수업 기록·차시 기록·이 교시 기록·과제(평가)·평가 과제·수행평가 라벨·RAW 근거)를 상시 검출. 과제 노트 제목 고유명("수행평가 - ")·평가방법 입력 예시는 허용 예외
- 가드 도입과 함께 잔존 위반 8건 정리: 학생부 근거 알림·버튼·검토 문구 "RAW 근거"→"학생부 근거 (RAW)" 6곳, 단원 설계 라벨 "수행·평가 과제"→"수행과제" 2곳(모달·노트 본문 — 본문은 파생이라 기존 노트와 왕복 충돌 없음)
- 설정 탭 포함 전 소스가 검사 범위이므로 백로그 ○ "설정 탭 용어 전수 검토"도 함께 완료

## 1.26.0 - 2026-07-22

### 추가 — 터치 길게 누르기 = 편집 메뉴 (백로그 ◐ / UIUX §1 터치 등가)

- **시간표·오늘 패널 교시**: 길게 누르면(0.5초) 우클릭과 같은 편집 컨텍스트 메뉴(과목 목록·수업 삭제·변경 제거·진도 고정·편집…)가 손끝 위치에 열림. 10px 이상 움직이면 스크롤로 판단해 취소, 메뉴가 열린 뒤의 탭은 삼켜 인스펙터가 겹쳐 열리지 않음
- **간트 막대·과제 ◆·행사 ●**: 길게 누르면 전체 라벨과 열기 동작을 담은 메뉴 표시 — 호버 title을 볼 수 없는 터치 환경에서 작은 마커의 정보·이동 경로 확보
- `registerLongPress` 헬퍼를 `@core/dom`에 추가, 길게 누르기 대상에 iOS 콜아웃·텍스트 선택 억제 CSS

## 1.25.0 - 2026-07-22

### 추가 — 계획 수립 도구 3종 (백로그 ★ / PRODUCT §10 R0)

LLM 없이도 학년·학기초 워크플로 0단계 4~6을 수행하는 단독 경로. 협업 경로(스킬)를 대체하지 않고 보완한다.

- **일반 단원 일괄 생성** (모달) — 진도표를 과목·단원별로 묶어 단원 노트 초안 생성: 전개(차시 흐름·배정일), 기간·시수, 성취기준, 통합 이관 차시 `→ 통합 운영` 표시. 과목별 미리보기(단원 수·기존 건너뜀)와 토글 선택, 같은 이름 단원은 건너뜀(멱등)
- **평가 계획 가져오기** (모달) — 평가계획 표 붙여넣기(시기|단원|평가 요소|평가 기준|평가 방법)로 과제 노트 일괄 생성+단원 연계+진도표 과제 열 기입. "9월 3주" 같은 주차 시기는 그 주 배정 차시 날짜로 매칭(단원 일치 우선), 같은 날짜·이름은 건너뜀
- **성취기준 노트 생성** (명령) — 두 학기 진도표·단원 노트의 코드를 모아 없는 노트만 생성(전문은 수기 보완 안내 포함, 기존 노트 불변)
- **진도표 성취기준 링크화** (명령) — `[코드]`·맨 코드를 `[[코드]]`로 일괄 전환(재실행 안전)
- 내비게이터 교육과정 운영 작업에 4종 추가, 명령 팔레트 4종, core `planning.ts` 신설(테스트 9개), 실볼트 읽기 전용 검증(단원 86 인식·링크화 0건 변경 멱등 확인)
- 문서: UX-FLOWS 워크플로 0 실행 경로 갱신, PRODUCT R0 구현 표시, UIUX 백로그 체크

## 1.24.5 - 2026-07-22

### 수정 — 간격 스케일 정규화 마무리 (1.24.1 누락분)

- DESIGN §5.2 스케일(4/6/8/12/16/24) 밖 margin·padding·gap 49건을 전수 스캔으로 찾아 정리, 재스캔 0건 확인
- 뷰 바깥 여백 24px 통일: 캘린더(20), 할 일·루틴·보고서·데이터·보존(22), 대시보드 헤더 아래(22)
- 모달·툴바 리듬 정리: 출결·회신 툴바 14/10→12/8, 과제 요약·툴바 14→12·10→8, 카드 내부 14→12(교육과정 지표·개념 단계·차시 목록, 패널 h3, 루틴 내비·체크리스트, 보고서 동작, 오늘 섹션, 인스펙터 요약 — 데이터 그리드는 섹션 경계라 16)
- 타임라인 정렬 재계산: 마커 열 20→16px로 날짜 들여쓰기 28→24px 정렬 유지, 날짜 위·아래 20/9→16/8, 항목 간 10→8, 마커 위 14→16(본문 첫 줄 중앙), 문단 7→6
- 미세값: 배지·오늘 버튼 패딩 3→4px, 배지 간격 5→6, 기록 메타 gap 3→4, 제목-부제 5→4, 경고·경로 목록 들여쓰기 20→16·34→24

## 1.24.4 - 2026-07-22

### 수정 — 버튼 묶음 간격 8px 통일 (전수 점검 후속)

- 화면마다 4·6·12px로 혼재하던 버튼 묶음 gap 17곳을 8px로 통일: 캘린더·루틴 내비게이션, 간트 툴바, 운영 설정 행·주 이동, 학생·차시 인스펙터, 데이터 도구, 보고서 기간 프리셋, 학생부 커버리지, 출결·과제·회신 툴바, 보존 정리, 레거시·학생부 검토 행, GTD 카드 제목
- 과제 모달은 제목 묶음(텍스트 쌍, 12px)과 툴바(버튼, 8px)를 분리해 적용
- 세로 목록·카드·그리드 간격(6·12·16px)은 버튼 묶음이 아니므로 유지

## 1.24.3 - 2026-07-22

### 수정 — 통합 목록 원본 열 버튼 간격 (스크린샷 점검)

- 원본 열의 열기·타임라인 버튼이 붙어 있던 것을 기존 `class-management-actions` 묶음(간격 8px)으로 감싸 분리

## 1.24.2 - 2026-07-22

### 수정 — GTD 할 일 뷰 디자인 정합 (스크린샷 점검)

- **필터에 항상 보이는 라벨 추가(§7.3)** — 검색·프로젝트·컨텍스트가 placeholder·aria-label에만 의존하던 것을 통합 목록과 같은 필터 바 패턴으로 교체. 검색 placeholder는 라벨 중복을 피해 예시("예: 상담 준비")로 변경
- `filterLabel` 헬퍼를 통합 목록 전용에서 `@core/dom` 공용으로 승격 — 두 뷰가 같은 코드 사용
- **빈 보드 다음 행동 안내(§7.9)** — 할 일이 하나도 없으면 "`할 일 수집`으로 첫 항목을 추가하세요", 필터 결과만 없으면 "검색어나 필터를 지워 보세요"를 보드 위에 표시
- 빈 열 최소 높이 320px → 160px — 내용 없는 보드의 과도한 세로 여백 축소

## 1.24.1 - 2026-07-22

### 수정 — 버튼·카드 간격과 여백 정합 (스크린샷 점검)

- **대시보드 명단·최근 기록 행이 가운데 정렬되던 버그 수정** — Obsidian 버튼 기본 정렬을 덮지 않아 생긴 문제. 왼쪽 정렬로 교정
- 학생별 타임라인 버튼의 호버 전용 노출(opacity 0) 제거 — 항상 보이되 보조 톤(§13.1 호버 전용 행동 금지)
- **간격 스케일 전면 정규화(67개 선언)** — 5·7·9·10·11·13·14·18px 등 임의 값을 §5.2 스케일(4/6/8/12/16/24)로: 요약 카드(gap 4·padding 16), 패널(16), 필터 바(gap 12·라벨 4), 통합 목록 표 셀(8×12), 뷰 액션 묶음(8), 모달 안내문(8×12) 등

## 1.24.0 - 2026-07-22

### 개선 — 디자인 가이드(docs/DESIGN.md) 정합

- **장식 제거**: 대시보드·일체화 뷰의 배경 그라디언트, 카드 그림자(0 8px 24px), 네비 활성의 inset 색 띠 제거 — 평면 위계(테두리·배경·글꼴)로 대체
- **좌측 강조선 카드 철거(12곳)**: 캘린더 이벤트·통신문 카드는 색 점(●) 표식으로, 모달 안내문(blockquote 3곳)·고정 안내는 평면 테두리/배경 tint로 교체
- **테마 변수 준수**: 하드코딩 색 fallback(#e5c07b·#d19a66) 제거, 간트 경과 음영 rgba→`color-mix`, 캘린더 오늘 표시는 외곽선+배경 tint로
- **간트 접근성·반응형**: 막대·라벨·과제 ◆·행사 ● 키보드 접근(Enter/Space·aria-label·focus-visible), 라벨 폭 CSS 변수화(`--class-management-gantt-label`), 700px 이하에서 라벨 축소·행 40px 터치 타깃
- 네비게이터 활성 항목에 `aria-current="page"`
- 수업 없음 빗금(패턴)은 의미 전달용이므로 유지

## 1.23.0 - 2026-07-22

### 구조 — 코어 패키지 분리·제품 문서화

- **`packages/core` 분리** — 순수 도메인 모듈 24종(학사일정·시간표·진도·단원·시수 감사·주간안내·노트 직렬화 등)을 플랫폼 독립 워크스페이스 패키지로 이동. 플러그인은 `@core/*` 별칭으로 사용하며, `tests/core-purity.test.mjs`가 "옵시디언은 import type만·코어 밖 참조 금지" 경계를 강제(테스트 94개)
- **설계 문서 3종 신설** — [docs/PRODUCT.md](docs/PRODUCT.md)(목표·비목표·아키텍처 3원칙·kind 22종 데이터 모델·기능 맵·일체화 그래프), [docs/UX-FLOWS.md](docs/UX-FLOWS.md)(3분할 레이아웃·뷰 16종 지도·핵심 워크플로 6), [docs/UIUX-PRINCIPLES.md](docs/UIUX-PRINCIPLES.md)(상호작용 규약·용어 사전·알림 문형·시각 언어·데이터 안전·리팩토링 백로그)
- 원칙 정합: 우클릭 메뉴의 수업일지 항목이 기존 여부에 따라 `쓰기/열기`로 표시, 시간표 칸 모달 제목을 `N교시 편집`으로 통일

## 1.22.1 - 2026-07-22

### 수정 — 간트 오늘 선

- 오늘 선 위치 계산의 CSS 오류(백분율 곱셈)로 선이 표시되지 않던 문제 수정 — 트랙 좌표계 오버레이 방식으로 재구현하고 선 위에 `오늘` 깃발 라벨 추가
- 툴바에 오늘 날짜 칩 표시 — 오늘이 학기 범위 밖(방학)이면 `학기 범위 밖(방학)`으로 안내 (선은 학기 중에만 그려짐)

## 1.22.0 - 2026-07-22

### 추가 — 일체화 간트 뷰

- **일체화 간트** (`일체화 간트 열기` 명령·학급 메뉴) — 학기 시간축 위에 과목별 단원 막대를 그려 교육과정 흐름을 한눈에 표시:
  - 일반 단원(회색)·통합 단원(강조색, ✦ 아이콘) 구분, 막대 안에 계획 시수, 경과 구간 음영
  - 수행평가(◆ 주황)·행사(● 보라) 마커, 오늘 위치 세로선, 월 라벨·주 눈금
  - 막대·이름 클릭 → 단원 노트, ◆ 클릭 → 과제 노트, ● 클릭 → 행사 노트
  - 학기 전환(1·2학기)과 `통합 단원만` 필터, 데이터 변경 시 자동 갱신
  - 단원 연계가 없는 수행평가는 과목·기간이 맞는 일반 단원 행에 표시

## 1.21.0 - 2026-07-22

### 추가 — 근거 연결 보완·용어 통일

- **차시 인스펙터 `학생부 근거` 버튼** — 학생 검색 → 날짜·과목·단원(기간 자동 매칭)·수업일지가 미리 채워진 근거 모달로 진입
- **근거 모달의 수업일지 연결 개선** — 단원 없이도 수업일지 선택 가능(허브 수업일지 포함), 단원 선택 시 그 단원+단원 없는 수업일지 목록, 단원 연계 수업일지를 고르면 단원도 자동 연결, 날짜 역순 정렬·라벨 개선
- **용어 통일**: 수업 기록·이 교시 기록·차시 기록 → **수업일지** / 과제(평가)·수행평가·평가 과제(UI 라벨) → **과제** / 통합 단원(일반 단원 포함을 가리킬 때) → **단원**, RAW 근거 저장 → 학생부 근거 저장 (RAW). 진도표 열 헤더 `과제(평가)`→`과제`(이름 기반 해석이라 기존 노트 호환), Bases `통합 단원.base`→`단원.base`(일반/통합 구분 열 포함)
- 차시 인스펙터 '연결된 단원'이 해당 날짜가 기간에 드는 단원만 표시(일반 단원 43종 추가에 따른 정리)

## 1.20.0 - 2026-07-22

### 개선 — 과제 노트의 사용자 섹션 보존

- 과제 확인표를 다시 저장할 때(모달에서 체크 수정 등) 표 아래의 `##` 섹션(평가 정보·기준·메모 등)을 보존 — 수행평가 노트의 평가 정보가 체크 저장으로 사라지지 않음

## 1.19.0 - 2026-07-22

### 추가 — 진도표 성취기준·연계 열

- **진도표 11열 형식**: `고정 | 순 | 배정 | 단원·영역 | 학습 내용 | 시수 | 성취기준 | 통합 단원 | 과제(평가) | 준비물 | 비고` — 열 위치를 헤더 이름으로 해석해 이전 형식(9열·V1.10·레거시) 노트도 그대로 읽힘
- **성취기준 자동 기입**: 지도서 PDF의 단원 페이지 범위(목차 기반)에서 추출한 2022 개정 성취기준 코드를 826개 차시 행에 기입(9과목 × 2학기, 행사·창체 행 제외). 체육은 중단원 범위, 음악은 제재곡 페이지 정밀 추출, 영어는 상위 빈도 6개
- **통합 단원 열**: 6개 프로젝트 단원과 연계된 137개 차시 행에 위키링크 기입(예: 사회1·도덕5·국어2·미술12 → 달라서 아름다운 우리, 봉사활동 → 따뜻한 세상 만들기)
- **과제(평가) 열**: 단원 연계 과제를 저장하면 그 날짜에 배정된 해당 과목 차시 행에 과제 링크가 자동 기입됨
- 비고 링크 기록을 `appendProgressRowLink`(비고·통합 단원·과제 공용)로 일반화

## 1.18.0 - 2026-07-22

### 변경 — 수업 기록 허브 재정의 (실시 차시 원장 회수)

- **수업일지 = 차시의 허브 노트**로 재정의: 단원 연계가 필수에서 **선택**으로 바뀌고(모달에 연계 단원 드롭다운·과목 입력 추가), 날짜·과목만으로 가볍게 시작해 메모→평가 증거→성찰로 채워 갈 수 있음. 학생 관찰·과제가 이 노트를 링크하는 구조
- **원클릭 진입** — 차시 인스펙터 `이 교시 기록`(기록이 있으면 `수업 기록 열기`)과 시간표·오늘 패널 우클릭 메뉴의 `이 교시 기록 (수업일지)`: 이미 있으면 노트를 열고, 없으면 날짜·교시·과목·진도 내용이 채워진 모달을 열며 기간·과목이 맞는 단원을 자동 제안
- **익일 RAW 확정** — 날짜가 지난 수업 기록에 플러그인이 `recordStatus: raw`를 스탬프하고 이후 절대 다시 쓰지 않음(수정하지 않는 관례). 편집해도 raw 표식은 보존
- **본문 경량화** — 수업일지 노트가 빈 섹션을 렌더링하지 않음(내용이 없으면 `## 기록`만). 단원 없는 기록은 단원 줄 생략
- **실시 차시(전 교시 자동 원장) 기능 회수** — 1.17.0의 확정 명령·노트·Bases 보기 제거. 전 교시 빈 노트는 정보가 아니라 소음이며, 시수·이력 조회는 기존 원장 뷰가 담당
- 수업일지 Bases 보기에 `날짜별` 보기(recordStatus 열 포함) 추가, `단원별 차시`는 단원 연계된 기록만 표시
- 단원 진행률(taughtHours)은 **실행 완료** 수업일지만 집계하도록 정리(단원 변경 시 이전 단원도 재계산)

## 1.17.0 - 2026-07-22

### 추가 — 하이브리드 심화: 실시 차시 확정·행사 일괄·Bases 확장

- **실시 차시 확정** — `실시 차시 확정 (어제까지)` 명령이 어제까지 실시된 차시를 `교육과정/실시 차시/{월}/` 아래 불변 노트로 생성(날짜·교시·과목·단원·학습 내용·시수·성취기준·수업일지 링크). 과거는 재배정되지 않으므로 한 번 만든 노트는 다시 쓰지 않음 — Bases 전면 전환의 걸림돌(쓰기 증폭) 없이 원장을 노트로 축적
  - 플러그인 로드 시 밀린 날이 14일 이하면 자동 확정, 그보다 많으면 안내만 표시
  - 과목 없는 행사 교시·삭제 교시는 수업이 아니므로 제외
- **행사 노트 일괄 만들기** — 학사일정의 모든 행사 노트를 한 번에 생성하는 명령
- **Bases 보기 3종 추가** — 주간학습안내(주차별), 실시 차시(날짜별·과목별·성취기준별), 연계 기록(단원 노트 임베드용 — 이 단원을 링크한 수업일지·과제·근거 자동 수집)
- **단원 진행률** — 수업일지 저장 시 단원 노트의 `taughtHours` 자동 갱신, 통합 단원 Bases 보기에 실시 시수·진행률(%) 열 추가
- 통합 단원 본문에 `6. 연계 기록` 섹션(연계 기록.base 임베드) 추가
- Bases 보기·행사 노트 템플릿을 순수 모듈(entity-notes.ts)로 이동 — 플러그인과 스크립트가 같은 단일 진실 사용

## 1.16.0 - 2026-07-22

### 변경 — 시간표 칸 조작 방식 분리 (좌클릭 보기·우클릭 편집)

- **좌클릭**: 주간 시간표·오늘 패널의 교시를 클릭하면 차시 인스펙터만 열림 (과목 변경 모달이 함께 뜨지 않음)
- **우클릭**: 컨텍스트 메뉴로 바로 수정 — 과목 목록(현재 과목 ✓, 선택 즉시 변경), 수업 삭제(이날만), 변경 제거(원래대로), 진도 고정(고정된 차시는 📌로 표시), 편집…(기존 과목 변경 모달: 직접 입력·사유 포함)
- 빈 칸·삭제된 교시도 우클릭 메뉴에서 과목 선택으로 바로 추가·복원
- 키보드(Enter·Space)는 좌클릭과 동일하게 차시 인스펙터를 열고, 상세 편집은 인스펙터의 `이 교시 편집` 버튼 이용

## 1.15.0 - 2026-07-22

### 추가 — 교육과정-수업-평가-기록 일체화 연계 층

- **차시 승격(수업일지 만들기)** — 차시 인스펙터의 `수업일지 만들기` 버튼으로 진도표 차시를 수업일지 노트로 승격: 통합 단원 검색 → 날짜·교시·시수·학습 내용이 미리 채워진 수업일지 작성 → 저장 시 진도표 비고에 `[[수업일지]]` 위키링크 자동 기록
- **행사 노트** — 차시 인스펙터의 '이날 행사'와 오늘 패널의 '다가오는 일정'에서 행사를 클릭하면 `교육과정/행사/`에 노트 자동 생성·열기(계획·준비물·연계 단원·결과 성찰 틀 포함)
- **과제-단원 연계** — 과제(평가) 모달에 `연계 단원` 선택 추가: 저장 시 frontmatter(curriculumUnitId·Title·Path)와 본문에 단원 위키링크 기록
- **일체화 Bases 보기** — `일체화 Bases 보기 만들기` 명령으로 `교육과정/뷰/`에 5개 .base 생성: 통합 단원(과목별), 수업일지(단원별 차시), 과제(평가 과제·단원 연계만), 행사 일지, 학생부 근거(영역별·검토 대기)
- 통합 단원 노트에 `class-management/curriculum-unit` 태그 추가(Bases 필터용)

## 1.14.1 - 2026-07-22

### 개선

- 주간 시간표 열 너비 균등화 — 교시 열은 내용 폭으로 고정, 요일 열은 동일 너비로 분배(긴 학습 내용은 줄바꿈)
- 시수 점검표도 교과·영역 열만 넓게, 숫자 열은 균등 정렬

## 1.14.0 - 2026-07-22

### 추가

- **학생 인스펙터** (오른쪽 패널) — 대시보드 명단 클릭 또는 `학생 인스펙터 열기` 명령으로 학생 요약 확인: 30일 기록·출결 예외·과제 예외·학생부 근거 카드, 근거 영역 분포(창체·교과·행동), 최근 기록·출결 예외·과제 예외 목록(클릭 시 원본), 빠른 기록·학생부 근거·타임라인·학생 노트 바로가기
- **차시 인스펙터** (오른쪽 패널) — 주간 시간표·오늘 패널의 교시를 클릭하면 열려 있는 인스펙터가 조용히 따라오며 해당 차시 상세 표시: 과목·변경/행사 여부, 진도 차시(단원·시수·성취기준·준비물·배정·📌), 이날 행사, 같은 과목의 통합 단원 링크, 이 교시 편집·진도표 열기 버튼
- 학급 메뉴의 대시보드·교육과정 운영 작업에 인스펙터 진입점 추가

## 1.13.0 - 2026-07-22

### 추가

- **오른쪽 '오늘' 패널** — 아침에 하루가 한눈에 보이는 상시 패널:
  - 오늘 날짜·학사 상태(수업일/휴업일 명칭/방학)
  - 오늘 시간표: 교시별 과목 + 배정 진도 차시(📌 고정 표시), 행 클릭으로 바로 편집
  - 출결: 미체크 경고 또는 상태별 집계 + 출결 체크 버튼
  - 오늘 마감·할 일: 기한 지난·오늘 마감 할 일과 가정통신문 회신 마감(D-day)
  - 루틴 완료 현황, 다가오는 일정(2주 내 행사·휴업일 D-day)
- 플러그인 로드 시 오른쪽 패널에 자동으로 열리며 `오늘 패널 열기` 명령 제공, 데이터 변경 시 자동 갱신

## 1.12.1 - 2026-07-21

### 개선

- 학급 대시보드에서 빠른 작업·뷰 열기 버튼 구역 제거 — 요약·명단·최근 기록 중심으로 정돈 (명령은 왼쪽 학급 메뉴가 담당)
- 학급 메뉴 패널을 Obsidian 네이티브 스타일로 재설계: 항목별 아이콘, 투명 배경·호버 강조, 활성 뷰 액센트 바, 구역 구분선·소제목, 키보드 탐색

## 1.12.0 - 2026-07-21

### 추가

- **왼쪽 사이드 패널 '학급 메뉴'** — ① 뷰 진입점(대시보드→교육과정 운영→캘린더→… 작업 순서 배열, 활성 뷰 강조) ② 현재 화면의 작업(뷰별 명령을 작업 순서·빈도순으로) ③ 자주 쓰는 명령(중복 제외). 플러그인 로드 시 자동으로 왼쪽 패널에 열리며 `학급 메뉴 열기` 명령으로 다시 열 수 있음
- 학급 대시보드 상단을 **빠른 작업 / 뷰 열기** 두 구역으로 재구성 (교육과정 운영·백업·유지관리 진입점 추가)

## 1.11.0 - 2026-07-21

### 추가

- 주간 시간표에서 칸을 수정(과목 변경·교시 추가·삭제·복원)하면 **영향받은 과목의 진도 배정이 자동 갱신** — 변경 전·후 과목의 진도표만 다시 계산해 배정 열에 기록하고, 알림에 `진도 재배정`(확인 필요 건수 포함) 표시
- 진도표 노트를 직접 편집한 경우에는 기존처럼 `진도 자동 배정` 명령으로 갱신

## 1.10.1 - 2026-07-21

### 변경

- 진도표 열 순서를 `고정 | 순 | 배정 | …`으로 조정 (📌가 맨 앞). 1.10.0 순서와 그 이전 순서 노트도 계속 자동 인식
- 차시 고정 검색 목록도 위치(📌/배정) — 순·단원·차시 순으로 표기

## 1.10.0 - 2026-07-21

### 변경

- 진도표 열 순서를 `순 | 고정 | 배정 | 단원·영역 | 학습 내용 | 시수 | 성취기준 | 준비물 | 비고`로 변경 — 고정·배정 상태를 표 앞에서 바로 확인
- 기존 열 순서 노트도 헤더를 보고 자동 인식 (다음 재배정 때 새 순서로 정리)
- 차시 고정 검색 목록도 같은 순서(순 — 📌 위치/배정 — 단원·차시)로 표기하고 '고정'·'배정' 접두어 제거

## 1.9.0 - 2026-07-21

### 변경

- 진도표 열 정리: `고정 날짜` → `고정`. 고정된 차시는 `📌` 표식만 남기고 위치는 `배정` 열이 담당 (배정과 다를 때만 `📌 2026-10-15(3)`로 명시해 위치를 잃지 않음)
- `배정` 열에 연도 포함 (`10-15(3)` → `2026-10-15(3)`) — 12월~1월에 걸친 학기에서도 모호하지 않음
- 이전 표기(`2026-10-15(3)` 단독, `고정 날짜` 헤더)는 그대로 읽히며 다음 재배정 때 새 형식으로 정리됨

## 1.8.1 - 2026-07-21

### 개선

- 고정된 차시 강조 표시: 주간 시간표 칸(📌·강조색·툴팁), 칸 편집 모달(고정 차시 안내 배너), 차시 고정 검색 목록(이 교시 고정 행 배경 강조, 다른 곳 고정 행 📌·강조색)

## 1.8.0 - 2026-07-21

### 추가

- 주간 시간표 칸에서 **날짜-진도 고정** — 칸 클릭 → '이 교시에 차시 고정' → 그 과목 진도표의 차시를 검색해 선택하면 해당 날짜·교시에 고정되고 그 과목이 즉시 재배정됨
- 이미 이 교시에 고정된 차시를 다시 선택하면 고정 해제, 다른 곳에 고정된 차시·현재 배정 위치가 목록에 표시됨

## 1.7.0 - 2026-07-21

### 추가

- 진도표 `고정 날짜` 열이 교시까지 지원 — `2026-10-15(3)` 또는 `2026-10-15 3교시`로 적으면 그 차시가 해당 날짜·교시에 정확히 배정 (행사 연계 차시, 외부 강사 수업 대응)
- 배정 우선순위: 날짜+교시 고정 → 날짜 고정 → 순차. 고정 교시에 해당 과목 수업이 없으면 배정하지 않고 "주간 시간표에서 과목을 먼저 배치하세요" 경고
- 시수 2 이상인 교시 고정 차시는 그 교시부터 연속 배정

## 1.6.1 - 2026-07-21

### 수정

- 주간 시간표가 모든 주를 "현재 설정된 학기"의 시간표로 해석하던 문제 수정 — 이제 각 날짜가 속한 학기의 기초시간표·변경 기록으로 해석 (학기 설정을 바꿔도 지난 학기 주간 시간표가 달라지지 않음)
- 칸 클릭 변경·삭제가 날짜가 속한 학기의 기초시간표 노트에 기록되도록 수정 (설정 학기와 무관), 알림에 기록된 학기 표시
- 주간학습안내 생성도 날짜별 학기의 시간표·진도표 사용, 학기 경계 주 지원
- 진도 자동 배정 알림에 대상 학기 명시 (배정은 원래도 설정 학기의 진도표에만 적용됨)

## 1.6.0 - 2026-07-21

### 변경

- 시수 점검의 **실행** 열을 "오늘까지 시간표 기준으로 운영된 시수"로 변경 (시간표 변경·행사·교시 삭제 반영). 기존의 수업일지 완료 차시 집계는 모든 수업을 수업일지로 쓰지 않는 실사용에서 항상 0에 가까워 대체
- 과목이 지정되지 않은 행사 교시는 시간표에 행사명으로 표시하되 교과 시수 집계·진도 배정에서 제외 — 시수 점검표에 행사명이 "기준 없음" 행으로 나타나던 문제 해소

## 1.5.1 - 2026-07-21

### 수정

- 빈 칸에 수업을 추가할 때 드롭다운의 첫 과목(국어 등)을 그대로 두고 저장하면 등록되지 않던 문제 수정 (표시 값과 내부 상태 동기화)
- 과목 선택 목록이 기준 시수 노트를 우선하도록 변경 — 설정의 일반 교과 목록과 학교자율시간 실제 과목명(예: 디지털 놀이터)이 중복 표시되던 문제 해소. 기준 시수·시간표·진도표가 없을 때만 설정의 교과 목록을 사용

## 1.5.0 - 2026-07-21

### 추가

- 주간 시간표 칸에서 **이 교시 삭제** — 기초시간표는 5교시지만 그날만 4교시로 운영하는 경우 대응
- 삭제는 `시간표 변경` 표에 `－` 표식으로 기록되며(`-`·`삭제`도 인식), 삭제된 칸은 빗금과 "수업 없음"으로 표시
- 삭제된 칸 클릭 → 변경 제거로 원래 시간표 복원 또는 다른 과목 재배정
- 삭제된 교시는 진도 배정·편성 시수·주간학습안내에서 제외

## 1.4.0 - 2026-07-21

### 변경

- 시간표 우선순위를 `시간표 변경 > 행사 > 기초시간표`로 조정 — 행사(체육대회·전일 체험학습 등)가 배정된 날에도 칸 클릭으로 특정 교시를 교과·창체로 재배정 가능
- 행사 교시 클릭 시 차단 대신 편집 모달을 열고, 변경 제거 시 행사 배정으로 복귀

## 1.3.0 - 2026-07-21

### 추가

- 주간 시간표의 빈 칸(＋)을 클릭해 기준 교시 밖 수업 추가 — 5교시 요일의 6교시 운영, 체험학습 7·8교시(최대 8교시)까지 대응
- 기준 교시를 넘는 `시간표 변경` 행과 행사 교시는 그날의 교시를 자동 확장하며, 변경 제거 시 다시 사라짐
- 확장된 교시는 주간학습안내·진도 배정·편성 시수 집계에 그대로 반영

## 1.2.0 - 2026-07-21

### 추가

- 교육과정 운영 뷰의 주간 시간표에서 칸을 클릭해 그 날짜의 교시 과목을 바로 변경 (기초시간표 노트의 `시간표 변경` 표에 자동 기록, 같은 날짜·교시는 교체, 변경 제거 버튼 제공)
- 키보드(Tab·Enter·Space)로도 칸 편집 가능, 행사 교시는 학사일정 노트로 안내

## 1.1.0 - 2026-07-21

### 추가

- 연간 학사일정 노트: 학기 기간·휴업일·행사(수업 적용/전일/단축·연장), 요일별 기준 교시, 수업일수·수업가능시수 자동 계산
- 기준 시수 노트와 교과·영역별 기준·편성·실행 시수 3단 대조, 증감 허용 범위(기본 ±20%) 판정
- 학기별 기초시간표 노트와 날짜별 시간표 해석(행사·단축·연장·시간표 변경 반영)
- 과목·창체 진도표 노트, Excel·한글 붙여넣기 차시 가져오기, 고정 날짜를 지키는 수업 시간 자동 배정과 부족·잉여 경고
- 주간학습안내 자동 생성(시간표·진도·아침활동 루틴·준비물·가정통신문 회신 마감 결합)
- 교육과정 운영 뷰(준비 상태·학사 요약·주간 시간표·시수 점검·실행 버튼)와 명령 7종
- 학급 캘린더에 휴업일·방학·행사 표시

### 변경

- manifest author를 wakeyi로 변경하고 authorUrl 추가
- 내부 유틸 통합·순수화, 테스트를 도메인별 20개 파일(81개 명명 테스트)로 분리

## 1.0.0 - 2026-07-21

### 주요 기능

- 학생 명단 단일·CSV·TSV 입력, 재적·전출·졸업 상태와 번호 변경
- 출결 상태·사유, 과제 제출 상태·메모, 생활 기록과 학생 위키링크
- 통합 목록·검색·복합 필터·저장된 보기·학생별 타임라인
- 월간·주간 캘린더와 날짜별 빠른 생성
- GTD 할 일, 가정통신문 회신, 일·주·월 루틴 체크리스트
- 분석 보고서, CSV 내보내기, 근거 링크가 있는 학생 피드백과 2026 기재요령 기반 학교생활기록부 영역별 검토 초안
- 창체·교과·행동 전용 구조화 근거, 학급 일괄 입력, 학생·교과별 누락 점검, RAW 검토 상태와 기존 기록 분류 추천
- `AGENTS.md`, `CLAUDE.md`, `AI_WORKFLOW.md` 기반의 선택적 파일 협업
- 다중 학급·학년도·학기, 다음 학기 명단 복사, 읽기 전용 보관
- 데이터 진단, 보관 기간 검토, Vault 내부 백업·비덮어쓰기 복구·레거시 마이그레이션
- 모바일 레이아웃, 키보드 탐색, 스크린리더 레이블, 제한 동시성 색인
- 첨부 교육과정 운영 자료를 반영한 성취기준 기반 통합 단원 설계, 백워드 설계, 일체화 자동 점검
- 차시별 수업 실행·과정중심 평가 증거·피드백·교사 성찰, 운영 시수 및 캘린더 연결
- 학생부 RAW 근거와 통합 단원·수업일지의 ID 및 위키링크 연결, CSV·데이터 진단 확장
- 2022 개정 교육과정 대비 개념기반 탐구학습 연구를 반영한 핵심 아이디어·개념적 렌즈·개념망·스트랜드 설계
- 스트랜드별 일반화와 사실적·개념적·논쟁적 안내 질문, 내용 지식·핵심 기능·평가방법, 분석적 루브릭
- 참여·집중·조사·조직·일반화·전이·성찰 차시 연결과 학생별 개념적 이해·탐구 과정·전이 증거 기록

## 0.9.0

- 성능, 모바일, 접근성, 백업·복구·마이그레이션 안정화

## 0.8.0

- 다중 학급·학기, 학생 재적 상태, CSV 내보내기, 데이터 진단과 보관 기간 관리

## 0.7.0

- 일·주·월 분석 보고서와 선택적 Codex·Claude 파일 협업

## 0.6.0

- GTD 할 일, 가정통신문 회신, 루틴 체크리스트

## 0.5.0

- 월간·주간 캘린더와 날짜별 빠른 생성

## 0.4.0

- 공통 활동 색인, 통합 목록·검색·필터, 학생 타임라인

## 0.3.0

- 과제 제출·미제출·보완 체크와 학생별 메모

## 0.2.0

- CSV·TSV 명렬표와 출결 상태·사유, Markdown 표 호환성

## 0.1.0

- 학생 노트, 생활 기록, 대시보드, Markdown 저장 구조
